export enum Resource {
  USER = "user",
}

export enum StatusEnum {
  INACTIVE = "inactive",
  ACTIVE = "active",
}

export enum UserTypeEnum {
  CUSTOMER = "customer",
  ADMIN = "admin",
}

export enum StageEnum {
  LOCAL = "local",
  DEVELOP = "develop",
}
